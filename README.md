# Telegram бот - Воронка "Антистресс"

Telegram бот для автоматической воронки продаж консультаций психолога с системой догревов.

## 🎯 Возможности

### Для пользователей:

- ✅ Получение полезного PDF-материала по кодовому слову
- ✅ Автоматическое предложение консультации психолога
- ✅ Простая отправка контактов для связи
- ✅ Система автоматических напоминаний

### Для администратора:

- ✅ Автоматический сбор контактов заинтересованных пользователей
- ✅ Уведомления о новых запросах на консультацию
- ✅ Статистика конверсии воронки
- ✅ Сегментированные рассылки (всем / без контакта / с контактом)
- ✅ Автоматические догревающие сообщения

## 📊 Логика воронки

### Этап 1: Вход в воронку

Пользователь вводит кодовое слово **"Антистресс"**

→ Бот отправляет:

- Приветственное сообщение
- PDF-файл с полезными материалами
- Сохраняет пользователя в базу

### Этап 2: Предложение консультации (через 1 минуту)

→ Автоматически отправляется сообщение с предложением консультации психолога со скидкой 30%

### Этап 3: Сбор контактов

Пользователь может отправить:

- Имя и телефон текстом (например: "Иван Петров +79991234567")
- Контакт через кнопку Telegram

→ Бот отправляет благодарность и уведомляет админа

### Этап 4: Автоматические догревы (если контакт не оставлен)

**Через 24 часа** - первое напоминание о важности заботы о психологическом здоровье

**Через 72 часа** - второе напоминание с акцентом на поддержку ребенка

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <url_репозитория>
cd bot_for_spam
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

1. Создайте файл `.env` на основе `env_example.txt`:

   ```bash
   cp env_example.txt .env
   ```

2. Получите токен бота у [@BotFather](https://t.me/BotFather) в Telegram

3. Узнайте свой Telegram ID у [@userinfobot](https://t.me/userinfobot)

4. Отредактируйте файл `.env`:
   ```
   BOT_TOKEN=ваш_токен_от_BotFather
   ADMIN_ID=ваш_telegram_id
   CODE_WORD=Антистресс
   PDF_FILE_PATH=путь_к_вашему_файлу.pdf
   ```

### 4. Подготовка PDF файла

Поместите PDF файл с материалами в директорию проекта и укажите путь к нему в `.env`

## 📝 Запуск бота

```bash
python bot.py
```

## 💬 Команды

### Для всех пользователей:

- `/start` - Начать работу с ботом
- `/help` - Справка по использованию
- `/id` - Проверить свой ID и статус в воронке
- **Антистресс** - Кодовое слово для получения материалов

### Для администратора:

- `/stats` - Статистика воронки (всего пользователей, конверсия)
- `/broadcast_all <текст>` - Рассылка ВСЕМ пользователям
- `/broadcast_no_contact <текст>` - Рассылка только тем, кто НЕ оставил контакт
- `/broadcast_with_contact <текст>` - Рассылка только тем, кто оставил контакт

#### Примеры рассылок:

```bash
# Рассылка всем
/broadcast_all Новая акция! Скидка 50% на вторую консультацию

# Догрев тех, кто не оставил контакт
/broadcast_no_contact Последний день скидки 30%! Не упустите возможность

# Напоминание тем, кто оставил контакт
/broadcast_with_contact Напоминаем, что вы можете записаться на консультацию по телефону
```

## 📁 Структура проекта

```
bot_for_spam/
├── bot.py              # Основной файл бота
├── config.py           # Конфигурация
├── database.py         # Работа с базой данных
├── messages.py         # Тексты сообщений воронки
├── requirements.txt    # Зависимости Python
├── env_example.txt     # Пример файла конфигурации
├── .env               # Ваша конфигурация (не коммитится)
├── .gitignore         # Исключения для git
├── users.db           # База данных (создаётся автоматически)
└── README.md          # Документация
```

## 🗄️ База данных

Бот использует SQLite для хранения информации о пользователях.

### Поля таблицы users:

| Поле              | Тип     | Описание                                                   |
| ----------------- | ------- | ---------------------------------------------------------- |
| user_id           | INTEGER | ID пользователя в Telegram (PRIMARY KEY)                   |
| username          | TEXT    | Username пользователя                                      |
| first_name        | TEXT    | Имя пользователя                                           |
| last_name         | TEXT    | Фамилия пользователя                                       |
| added_date        | TEXT    | Дата добавления в воронку                                  |
| status            | TEXT    | Статус в воронке (file_sent, offer_sent, contact_provided) |
| contact_provided  | INTEGER | Оставил ли контакт (0 или 1)                               |
| contact_name      | TEXT    | Имя для связи                                              |
| contact_phone     | TEXT    | Номер телефона                                             |
| last_message_time | TEXT    | Время последнего сообщения                                 |
| warmup_1_sent     | INTEGER | Отправлен ли первый догрев (0 или 1)                       |
| warmup_2_sent     | INTEGER | Отправлен ли второй догрев (0 или 1)                       |

## ⚙️ Настройка текстов сообщений

Все тексты сообщений хранятся в файле `messages.py`. Вы можете легко их изменить:

- `WELCOME_MESSAGE` - приветствие при входе в воронку
- `OFFER_MESSAGE` - предложение консультации (через 1 минуту)
- `THANK_YOU_MESSAGE` - благодарность после получения контакта
- `WARMUP_1_MESSAGE` - первый догрев (через 24 часа)
- `WARMUP_2_MESSAGE` - второй догрев (через 72 часа)
- `ADMIN_NOTIFICATION` - уведомление админу о новом контакте

## 🔄 Автоматические процессы

### Отложенные сообщения:

- **Через 1 минуту** после получения PDF → предложение консультации
- **Через 24 часа** без контакта → первый догрев
- **Через 72 часа** без контакта → второй догрев

### Фоновые задачи:

- Проверка пользователей для догрева - каждый час
- Автоматическая отправка догревающих сообщений

## 📈 Аналитика

Команда `/stats` показывает:

- Общее количество пользователей в воронке
- Количество оставивших контакт
- Количество без контакта
- Процент конверсии

## 🔒 Безопасность

⚠️ **Важно:**

- Никогда не публикуйте файл `.env` в открытом доступе
- Храните токен бота в секрете
- Регулярно проверяйте логи на наличие подозрительной активности
- База данных содержит персональные данные - защитите её

## 🐛 Устранение неполадок

### Бот не запускается

- Проверьте правильность токена в `.env`
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
- Проверьте, что не запущено несколько экземпляров бота одновременно

### PDF не отправляется

- Проверьте путь к файлу в `.env`
- Убедитесь, что файл существует
- Проверьте права доступа к файлу

### Догревы не отправляются

- Проверьте логи на наличие ошибок
- Убедитесь, что бот работает непрерывно
- Фоновая задача проверяет пользователей каждый час

### Контакты не распознаются

- Формат должен быть: "Имя Фамилия +79991234567"
- Или используйте кнопку отправки контакта в Telegram
- Проверьте, что номер телефона в правильном формате

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи бота
2. Убедитесь, что все настройки в `.env` правильные
3. Создайте Issue в репозитории проекта

## 📜 Лицензия

MIT

---

**Создано для автоматизации воронки продаж консультаций психолога через Telegram**
